/** @format */

export type PermissionStatus = 'granted' | 'pending';

/** Native permission steps surfaced in the Enable Permissions flow. */
export type PermissionId = 'usage-access' | 'display-over-apps' | 'notifications' | 'battery-optimization';
