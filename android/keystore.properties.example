# Copy to android/keystore.properties and fill in your Play upload key.
# android/keystore.properties is gitignored — a placeholder may already exist locally.
# Do not commit keystore.properties or .jks files.

storeFile=keystores/upload-keystore.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
