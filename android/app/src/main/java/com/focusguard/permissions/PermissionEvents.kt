package com.focusguard.permissions

/** Device events emitted when Android permission state may have changed. */
object PermissionEvents {
  const val PERMISSIONS_CHANGED_EVENT = "focusguard:permissionsChanged"
}
